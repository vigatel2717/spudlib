//
// Created by Nathan on 5/13/2026.
//

#ifndef SPUDLIB_SPUDGPUSWAPCHAIN_HPP
#define SPUDLIB_SPUDGPUSWAPCHAIN_HPP
#include <cstdint>

/*
namespace spud::gpu {
	class swap_chain {
	public:
		swap_chain() = default;
		virtual ~swap_chain() = default;

		virtual void recreate(
			const uint32_t &width,
			const uint32_t &height) = 0;

		virtual void set_vsync(const bool &vsync) = 0;
		[[nodiscard]] virtual bool is_vsync() = 0;

		[[nodiscard]] virtual uint32_t acquire_next_image() = 0;
		virtual void present() = 0;

		[[nodiscard]] virtual uint32_t get_width() const = 0;
		[[nodiscard]] virtual uint32_t get_height() const = 0;

		// Gets the native API object.
		[[nodiscard]] virtual uint64_t get_native_swapchain_object() const = 0;
	};
} */

#endif //SPUDLIB_SPUDGPUSWAPCHAIN_HPP
